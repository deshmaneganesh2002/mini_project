<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
          <div class="w-full flex justify end">

       <!-- Use a small form that looks like a button -->
<form method="POST" action="/logout" class="inline-block">
    <button type="submit" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">
        Logout
    </button>
</form>

          </div>  
        
      <h5 class="mb-5"> edit your post.</h5>
      <form method="POST" action="/update/<%= post._id %>">
        <textarea placeholder="what is on your mind to edit post ? " class="block p-3 outline-none /3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content%></textarea>
        <input class="px-3 w-40 py-2 text-sm bg-blue-500 block rounded-md mt-2" type="submit" value="Edit post">
    </form>
        
    </div>

    </div>
</body>
</html>