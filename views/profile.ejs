<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
          <div class="w-full flex justify end">

       <!-- Use a small form that looks like a button -->
<form method="POST" action="/logout" class="inline-block">
    <button type="submit" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">
        Logout
    </button>
</form>

          </div>  


      <h3 class="text-3xl mb-5"><span class="font-light">Hello, </span><%= user.name %> </h3>
        
      <h5 class="mb-5">you can create your own Blog.</h5>
      <form method="POST" action="/post">
        <textarea placeholder="what is on your mind ? " class="block p-3 outline-none /3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
        <input class="px-3 w-40 py-2 text-sm bg-blue-500 block rounded-md mt-2" type="submit" value="Create new Blog">
    </form>

        <h3 class="text-zinc-400">your Blogs </h3>
        <div class="postcontainer mt-5">
      <% user.posts.forEach(function(post){ %>

              <div class="post w-1/3 p-4 mb-4 border-2 border-zinc-800">
                <h4 class="text-blue-500 mb-2"><%= user.name %></h4>
                <p class="text-sm tracking-tight"><%= post.content %></p>
            
                <small><%= post.likes.length %> likes</small>

                  <div class="btns flex gap-4 mt-5">
                    <a class="text-blue-500" href="/like/<%= post._id  %>">
                        <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                    </a>

                </div>

                <div class="btns flex gap-4 mt-5">
                    <a class="text-blue-500" href="/edit/<%= post._id  %>">edit</a>

                </div>

                <div class="btns flex gap-4 mt-5">
                    <form method="POST" action="/delete/<%= post._id %>">
                     <input class="px-3 w-15 py-2 text-sm bg-red-500 block rounded-md mt-2" type="submit" value="delete post">
                 </form>

                </div>
                 
            </div>

      <% }) %> 

        </div>
    </div>

    </div>
</body>
</html>